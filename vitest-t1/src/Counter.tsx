export function Counter({ initalCount = 0 }) {
    // const {count, incrementCount, decrementCount} = useCount()

    return (
        <div>
            <button>-</button>
            <button>+ </button>
        </div>
    );
}
